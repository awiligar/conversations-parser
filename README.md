# conversations-parser

Conversation Parser is a lightweight, browser-based tool for working with conversation exports from LLM platforms (ChatGPT, Claude, and similar). Paste or upload an exported JSON file, select exactly the fields you care about, and generate a clean, custom-shaped JSON output — no server, no data leaving your browser.

## Run locally

```bash
python3 -m http.server 8000
```
